#include <stdio.h>

/**
 * main - Entry point
 * Description: use printf
 * Return: always 0
 */

int main(void)
{
printf("%s", "with proper grammer, but the outcome is a piece of art,\n");
return (0);
}
