#include <stdio.h>

/*
 * main - entry point
 * description:printsthe alphabet in lower case/n
 * Return: Always 0 (success)
 */

int main(void)
{int ch;
for (ch = 'a'; ch <= 'z' ; ch++)
putchar(ch);
putchar('\n');
return (0);
}
